apiVersion: v1
kind: Service
metadata:
  name: ecotrace-api
  namespace: ecotrace
  labels:
    app.kubernetes.io/name: ecotrace-api
    app.kubernetes.io/part-of: ecotrace-engine
spec:
  selector:
    app.kubernetes.io/name: ecotrace-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
